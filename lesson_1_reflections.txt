How did viewing a diff between two versions of a file hlep you see the bug that was introduced?

becuase you could compare a version that worked with a version that didn't 

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?

it becomes easier to debug errors and rollback changes

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, vs having versions automatically saved, like Google docs does?

cons: user error pros: can force a commit when you think its a good time to do it.

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file seperately?

They are used for different purposes

How can you use the commands git log and git diff to view the history of files?

use git stat --stat to see the changes

How might using version control make you more confident to make changes that could break something?

you can always rollback to a previous state if you do break something.

Now that you have your workspace set up, what do you want to try using Git for?

version control