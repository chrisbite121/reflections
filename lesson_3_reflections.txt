When would you want to use a remote repository rather than keeping all your work local?
when collaborating with others

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date wit your remote repository?

in case there are commits you do not want to pull

Describe the differences between forks, clones, and branches. When would you use one instead of another?

forks are copied responsitories from git hub to git hub, branches are new versions of code that run in isolation to other branches, forks are used to copy one github repository to another github repository, branches are used create features in parallel